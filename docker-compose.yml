version: '2'

services: 
  #keepalive:
  #  image: osixia/keepalived:1.4.5
  #  container_name: keepalived
  #  privileged: true
  #  volumes:
  #    - ./conf/keepalived/keepalived.conf:/usr/local/etc/keepalived/keepalived.conf
  #  network_mode: host
  #  logging:
  #      driver: "json-file"
  #      options:
  #         max-size: "10m"
  #         max-file: "2"

  nginx:
    image: nginx:1.14.0
    build:
        context: dockerfile/nginx
    container_name: nginx
    volumes:
        - ./conf/nginx/:/etc/nginx/
        - ./conf/tftpserver:/etc/xinetd.d
        - ./swi:/opt/
        - ./download:/opt/download
        - ./public:/opt/public
    #depends_on:
    #  - mars
    network_mode: host
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"

  elasticsearch:
    build:
        context: dockerfile/elasticsearch 
    image: elasticsearch:6.1.0         
    container_name: elasticsearch
    privileged: true
    network_mode: host
    volumes:
      - ./conf/elasticsearch/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - ./conf/elasticsearch/jvm.options:/usr/share/elasticsearch/config/jvm.options:ro
      - ./conf/elasticsearch/backup:/mnt/backup:rw
      - ./conf/elasticsearch/data:/usr/share/elasticsearch/data:rw
    #environment:
      #ES_JAVA_OPTS: "-Xmx4096m -Xms4096m"
      #network.bind_host: "127.0.0.1"
      #network.publish_host: "HOST_IP_TO_ONOS_IP"
      #cluster.name: "marscluster"
      #node.name: "es1"
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"

#  kibana:
#    image: docker.elastic.co/kibana/kibana-oss:6.1.0
#    container_name: kibana
#    privileged: true
#    network_mode: host
#    volumes:
#      - ./conf/kibana/kibana.yml:/usr/share/kibana/config/kibana.yml
#    logging:
#        driver: "json-file"
#        options:
#           max-size: "10m"
#           max-file: "2"
#    depends_on:
#      - elasticsearch


  logstash:
    build: 
      context: dockerfile/logstash   
    image: logstash:6.1.0      
    container_name: logstash
    privileged: true
    network_mode: host
    volumes:
      - ./conf/logstash/logstash.yml:/usr/share/logstash/config/logstash.yml:ro
      - ./conf/logstash/pipeline:/usr/share/logstash/pipeline:ro
    environment:
      LS_JAVA_OPTS: "-Xmx2g -Xms2g"
    depends_on:
      - elasticsearch
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"

  mars:
    image: mars:sia
    container_name: mars
    network_mode: host
    privileged: true
    mem_limit: 16g
    environment:
      - "ONOS_IP=HOST_IP_TO_ONOS_IP"
      - "JAVA_OPTS=-Xms16G -Xmx16G -XX:+UseConcMarkSweepGC -XX:+CMSIncrementalMode -Dcom.sun.management.jmxremote -Dds.lock.timeout.milliseconds=10000"
#     - "KARAF_DEBUG=true"
      - "ONOS_APPS=drivers,openflow,fwd,proxyarp,snmp,linkdiscovery\
        ,com.nocsys.alert\
        ,com.nocsys.analyzer\
        ,com.nocsys.monitor\
        ,com.nocsys.calcpath\
        ,com.nocsys.defaultcfg\
        ,org.onosproject.dhcprelay \
        ,com.nocsys.dhcpserver\
        ,com.nocsys.dhcpv6server\
        ,com.nocsys.drivers.nocsys.gnmi\
        ,com.nocsys.drivers.nocsys.rest\
        ,com.nocsys.drivers.nocsys.snmp\
        ,com.nocsys.eventclient\
        ,com.nocsys.healthycheck\
        ,com.nocsys.ntpserver\
        ,com.nocsys.provider.switchpolling\
        ,com.nocsys.qos\
        ,com.nocsys.switchmgmt\
        ,com.nocsys.useraccount\
        ,com.nocsys.utility\
        ,com.nocsys.staticrouting\
        ,com.nocsys.webssh"
    volumes:
      - ./conf/mars/modules:/etc/collectd/modules
      - ./conf/mars/mars_collectd.conf:/etc/collectd/collectd.conf
      - ./download:/root/onos/download
      - ./swi:/opt
      - /etc/localtime:/etc/localtime
      - /etc/timezone:/etc/timezone
    #depends_on:
    #  - elasticsearch
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"

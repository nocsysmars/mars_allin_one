version: '3.4'

services: 
  #keepalive:
  #  image: osixia/keepalived:1.4.5
  #  container_name: keepalived
  #  privileged: true
  #  volumes:
  #    - ./conf/keepalived/keepalived.conf:/usr/local/etc/keepalived/keepalived.conf
  #  network_mode: host
  #  logging:
  #      driver: "json-file"
  #      options:
  #         max-size: "10m"
  #         max-file: "2"

  nginx:
    image: nocsysmars/nginx:1.14.0-sia
    build:
        context: dockerfile/nginx
    container_name: nginx
    volumes:
        - ./conf/nginx/:/etc/nginx/
        - ./conf/tftpserver:/etc/xinetd.d
        - ./swi:/opt/
        - ./download:/opt/download
        - ./public:/opt/public
    #depends_on:
    #  - mars
    network_mode: host
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"

  elasticsearch:
    build:
        context: dockerfile/elasticsearch 
    image: nocsysmars/elasticsearch:6.1.0-sia
    container_name: elasticsearch
    privileged: true
    network_mode: host
    volumes:
      - ./conf/elasticsearch/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - ./conf/elasticsearch/jvm.options:/usr/share/elasticsearch/config/jvm.options:ro
      - ./conf/elasticsearch/backup:/mnt/backup:rw
      - ./conf/elasticsearch/data:/usr/share/elasticsearch/data:rw
    #environment:
      #ES_JAVA_OPTS: "-Xmx4096m -Xms4096m"
      #network.bind_host: "127.0.0.1"
      #network.publish_host: "HOST_IP_TO_ONOS_IP"
      #cluster.name: "marscluster"
      #node.name: "es1"
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"

#  kibana:
#    image: docker.elastic.co/kibana/kibana-oss:6.1.0
#    container_name: kibana
#    privileged: true
#    network_mode: host
#    volumes:
#      - ./conf/kibana/kibana.yml:/usr/share/kibana/config/kibana.yml
#    logging:
#        driver: "json-file"
#        options:
#           max-size: "10m"
#           max-file: "2"
#    depends_on:
#      - elasticsearch


  logstash:
    build: 
      context: dockerfile/logstash   
    image: nocsysmars/logstash:6.1.0-sia
    container_name: logstash
    privileged: true
    network_mode: host
    volumes:
      - ./conf/logstash/logstash.yml:/usr/share/logstash/config/logstash.yml:ro
      - ./conf/logstash/pipeline:/usr/share/logstash/pipeline:ro
    environment:
      LS_JAVA_OPTS: "-Xmx2g -Xms2g"
    depends_on:
      - elasticsearch
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"

  mars:
    image: nocsysmars/mars:sia
    container_name: mars
    network_mode: host
    privileged: true
    restart: always
    #mem_limit: 16g
    healthcheck:
      test: curl --fail -u marsinneruser:onlycodeused -I 'http://127.0.0.1:8181/mars/v1/network/configuration' || exit 1
      interval: 30s
      retries: 2
      start_period: 60s
      timeout: 5s
    environment:
      - "ONOS_IP=HOST_IP_TO_ONOS_IP"
      - "JAVA_OPTS=-server -Xms16G -Xmx16G -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+ParallelRefProcEnabled -XX:+DisableExplicitGC -XX:+AlwaysPreTouch -Dcom.sun.management.jmxremote -Dds.lock.timeout.milliseconds=10000"
#     - "KARAF_DEBUG=true"
      - "ONOS_APPS=drivers,openflow,fwd,proxyarp,snmp,linkdiscovery\
        ,com.nocsys.alert\
        ,com.nocsys.analyzer\
        ,com.nocsys.monitor\
        ,com.nocsys.calcpath\
        ,com.nocsys.defaultcfg\
        ,com.nocsys.drivers.nocsys.gnmi\
        ,com.nocsys.drivers.nocsys.rest\
        ,com.nocsys.drivers.nocsys.snmp\
        ,com.nocsys.eventclient\
        ,com.nocsys.healthycheck\
        ,com.nocsys.ntpserver\
        ,com.nocsys.provider.switchpolling\
        ,com.nocsys.storm-control \
        ,com.nocsys.qos\
        ,com.nocsys.switchmgmt\
        ,com.nocsys.useraccount\
        ,com.nocsys.utility\
        ,com.nocsys.webssh"
    volumes:
      - ./conf/mars/modules:/etc/collectd/modules
      - ./conf/mars/mars_collectd.conf:/etc/collectd/collectd.conf
      - ./download:/root/onos/download
      - ./swi:/opt
      - /etc/localtime:/etc/localtime
      - /etc/timezone:/etc/timezone
    #depends_on:
    #  - elasticsearch
    logging:
        driver: "json-file"
        options:
           max-size: "10m"
           max-file: "2"
